# Write a program that swaps the values of two variables.

first_variable = 10
second_variable = 20

print(f"Before swapping a = {first_variable}, b = {second_variable}")

# Swapping the values

first_variable, second_variable = second_variable, first_variable

print(f"After swapping a = {first_variable}, b = {second_variable}")

